spring.application.name=incident-management

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://aisha:1234@cluster0.ry1wmxf.mongodb.net/smartcampus_db

# Server Port
server.port=8080

# File Upload Setup
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=15MB

# Custom property for file storage path
file.upload-dir=uploads

# Allow cross origin requests (for dev)
spring.mvc.cors.mappings[/**].allowed-origins=*
spring.mvc.cors.mappings[/**].allowed-methods=*
